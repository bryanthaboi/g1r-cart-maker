{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Dialogs and opening links; every other capability is a Rust command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir"
  ]
}
